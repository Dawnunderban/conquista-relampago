import React from 'react';
import ReactDOM from 'react-dom/client';
import AuditorioApp from './pages/AuditorioApp';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AuditorioApp />
  </React.StrictMode>
);
